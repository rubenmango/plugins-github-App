/**
 * Stored for later: side-by-side DiffPane (incoming update vs live stage).
 * Keep off — review stays on a single canvas with a Design / Code toggle.
 *
 * When re-enabling, set to `true` and restore the `branchState === 'diff'`
 * transition in `+page.svelte` (`handleBranchPillClick`).
 */
export const ENABLE_BRANCH_DIFF_SPLIT = false;
